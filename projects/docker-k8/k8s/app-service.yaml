apiVersion: v1
kind: Service
metadata:
  name: ruby-form-app
  namespace: ruby-form
spec:
  type: NodePort
  selector:
    app: ruby-form-app
  ports:
    - port: 80
      targetPort: 4567
      nodePort: 30080
